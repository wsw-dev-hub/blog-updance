{
  "name": "blog-updance",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy  pages dist --project-name=blog-updance",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name=blog-updance --branch=preview",
    "deploy:staging": "npm run build && wrangler deploy --env staging",
    "post:new": "node scripts/new-post.js"
  },
  "devDependencies": {
    "vite": "^7.1.12",
    "vite-plugin-mkcert": "^2.1.0",
    "vite-plugin-node-polyfills": "^0.2.0",
    "wrangler": "^4.101.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mediapipe/holistic": "^0.5.1675471629",
    "@mediapipe/tasks-vision": "^0.10.15",
    "@ton/crypto": "^3.3.0",
    "@ton/ton": "^16.0.0",
    "@tonconnect/sdk": "^3.3.1",
    "@tonconnect/ui": "^2.3.1",
    "buffer": "^6.0.3",
    "tonweb": "^0.0.27"
  }
}
